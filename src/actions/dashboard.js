import { LOAD_TASKS_LIST, TAG_SELECTED } from './types';

export const loadTaskList = () => (dispatch) => {};

export const setTag = (tag) => (dispatch) => {};

export const add = (task) => (dispatch) => {};

export const remove = (id) => (dispatch) => {};

export const sortByTime = (order) => (dispatch) => {};

export const sortBy